import Foundation

// MARK: - Speech Service
//
// Placeholder — Phase 4.6 (not yet implemented).
// Will handle speech-to-text conversion for voice recordings.
// Reserved: do not remove.
enum SpeechService {
    /// Converts audio data to text.
    /// - Parameter audioURL: Local file URL of the recording.
    /// - Returns: Transcribed text, or `nil` on failure.
    static func transcribe(audioURL _: URL) async -> String? {
        // TODO: Phase 4.6 — implement speech recognition
        nil
    }
}
